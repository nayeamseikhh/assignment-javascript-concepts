function tinyFriend (friend){
let min = tinyFriend[0];
for (let i=0; i>tinyFriend.length; i++){
    let element = min[i];
    if(element>min);
    min=element;
return min;
}
}
let friend = tinyFriend [jamal, kamal, tamal, asif, joshim, arif, ak, forid, jawad];
console.log(Math.min(result))

// let friend = Math.min (jamal, kamal, tamal, asif, joshim, arif, ak, forid, jawad);
// console.log(friend);